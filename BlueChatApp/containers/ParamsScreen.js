import { createContext } from 'react';
import { StyleSheet, View, Text} from 'react-native';

export default function ParamsScreen() {

    return (
        <View>
            <Text style={{ textAlign: 'center', marginTop: 50 }}> Params page</Text>
        </View>
    )
}